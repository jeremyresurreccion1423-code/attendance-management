# Packaged overrides used by Railway when env vars are not set.
# Prefer Railway Variables for secrets when possible.

spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.zszwwwpzyepuduielccq
spring.datasource.password=Carlomercado@123

# Brevo — password/API key from Railway/env only (never hardcode)
spring.mail.username=${MAIL_USERNAME:b20400001@smtp-brevo.com}
spring.mail.password=${MAIL_PASSWORD:${BREVO_SMTP_PASSWORD:}}
central.mail.from-name=LU Centralized System
central.mail.from-email=edulibrary67@gmail.com
# Set BREVO_API_KEY in the environment (Railway Variables). Never commit the key.
central.mail.brevo-api-key=${BREVO_API_KEY:}

attendance.seed-on-startup=true
spring.thymeleaf.cache=false
